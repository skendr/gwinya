{{#if client}}"use client";

{{/if}}import { cn } from "@/lib/utils";

export function {{pascal name}}({ className }: { className?: string }) {
  return (
    <div className={cn("", className)}>
      {{pascal name}}
    </div>
  );
}
