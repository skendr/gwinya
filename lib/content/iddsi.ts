/**
 * The IDDSI framework data + verdict helpers now live in @gwinya/shared so the
 * web food scanner and the mobile one share the identical logic. Re-exported
 * here so existing `@/lib/content/iddsi` imports keep working unchanged.
 */
export * from "@gwinya/shared/content/iddsi";
