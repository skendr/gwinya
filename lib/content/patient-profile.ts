/**
 * The demo patient profile + `getDemoPlan()` now live in @gwinya/shared so web
 * and mobile share the identical demo data. Re-exported here so existing
 * `@/lib/content/patient-profile` imports keep working unchanged.
 */
export * from "@gwinya/shared/content/patient-profile";
